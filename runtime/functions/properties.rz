extends Node

var _speed := 200.0

var speed: float:
	get:
		return _speed
	set(value):
		_speed = maxf(0.0, value)

func test() -> void:
	prints("speed:", speed)
	speed = -10.0
	prints("clamped:", speed)
	speed = 300.0
	prints("set:", speed)

func _ready() -> void:
	test()
