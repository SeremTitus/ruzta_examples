class_name Player
extends CharacterBody2D

@export var speed := 220.0

func test() -> void:
	print("Player ready")

func _ready() -> void:
	test()

func _process(delta: float) -> void:
	position.x += speed * delta
